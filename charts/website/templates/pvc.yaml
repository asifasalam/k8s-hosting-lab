apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: {{ .Values.siteName }}-pvc
  namespace: {{ .Release.Namespace }}

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: standard

  resources:
    requests:
      storage: {{ .Values.storage.size }}
